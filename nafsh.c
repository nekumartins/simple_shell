#include <string.h>
#include "nafsh.h"

int main()
{
    char req[MAX];

    while (1)
    {
        printf("nafsh$ ");
        scanf(req);
        // if (req == "q")
        // {
        //     break;
        // }
        // fflush(stdin);
    }

    return (0);
}