#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX 1024

int main()
{
    char req[MAX];

    while (1)
    {
        printf("nafsh$ ");
        scanf(req);
        // if (req == "q")
        // {
        //     break;
        // }
        // fflush(stdin);
    }

    return (0);
}